const user = {
	name: "Name",
	name: "Email@gmaail.com"
}

// Export defaul -> chỉ có 1, ko có ngoặc có thì là theo name, là để dại diện 1 cái
// Có thể là dùng {} cho nhiều file
// có thể thay tên được ở import, vẫn nên theo tên của file để dễ bảo trì sau này
export default user;